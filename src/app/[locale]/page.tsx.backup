import { getTranslations } from 'next-intl/server';
import Hero from '@/components/features/Homepage/Hero';
import QuickLinks from '@/components/features/Homepage/QuickLinks';
import Header from '@/components/layout/Header';
import Footer from '@/components/layout/Footer';
import CookieConsent from '@/components/features/CookieConsent';
import SkipLink from '@/components/features/SkipLink';
import { ArrowRight, Calendar, Mail, Trophy, Music, Soccer, BookOpen, Users, Lightbulb } from 'lucide-react';
import Link from 'next/link';

export async function generateMetadata({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations({ locale: locale, namespace: 'homepage' });

  return {
    title: t('hero.title'),
    description: t('hero.description'),
  };
}

export default async function HomePage({ params }: { params: Promise<{ locale: string }> }) {
  const { locale } = await params;
  const t = await getTranslations();

  // Localized content
  const content = {
    pl: {
      aboutLabel: 'O nas',
      aboutTitle: 'Szkoła z tradycją,\nnowoczesna w duchu',
      aboutDescription: 'Od 1978 roku łączymy polską i niemiecką tradycję edukacyjną, tworząc miejsce, gdzie młodzi ludzie rozwijają się w otwartej, tolerancyjnej atmosferze. Nasza dwujęzyczna szkoła to przestrzeń spotkań, dialogu i wzajemnego poznawania się.',
      aboutCta: 'Poznaj historię WBS',
      valuesLabel: 'Co nas wyróżnia',
      valuesTitle: 'Edukacja bez granic',
      valuesDescription: 'WBS to więcej niż szkoła – to społeczność, która kształtuje przyszłych obywateli świata.',
      ctaTitle: 'Zapisz swoje dziecko do WBS',
      ctaDescription: 'Dołącz do społeczności, która od prawie 50 lat łączy kultury i tworzy wyjątkowe miejsce do nauki.',
      ctaPrimary: 'Umów wizytę',
      ctaSecondary: 'Pobierz prospekt',
    },
    de: {
      aboutLabel: 'Über uns',
      aboutTitle: 'Schule mit Tradition,\nmodern im Geist',
      aboutDescription: 'Seit 1978 verbinden wir polnische und deutsche Bildungstraditionen und schaffen einen Ort, an dem sich junge Menschen in einer offenen, toleranten Atmosphäre entwickeln. Unsere zweisprachige Schule ist ein Raum der Begegnung, des Dialogs und der gegenseitigen Entdeckung.',
      aboutCta: 'Lernen Sie WBS kennen',
      valuesLabel: 'Was uns auszeichnet',
      valuesTitle: 'Bildung ohne Grenzen',
      valuesDescription: 'WBS ist mehr als eine Schule – eine Gemeinschaft, die zukünftige Weltbürger formt.',
      ctaTitle: 'Melden Sie Ihr Kind an',
      ctaDescription: 'Werden Sie Teil einer Gemeinschaft, die seit fast 50 Jahren Kulturen verbindet.',
      ctaPrimary: 'Besuch vereinbaren',
      ctaSecondary: 'Prospekt herunterladen',
    },
    en: {
      aboutLabel: 'About us',
      aboutTitle: 'School with tradition,\nmodern in spirit',
      aboutDescription: 'Since 1978, we have been combining Polish and German educational traditions, creating a place where young people develop in an open, tolerant atmosphere. Our bilingual school is a space for encounters, dialogue, and mutual discovery.',
      aboutCta: 'Discover WBS history',
      valuesLabel: 'What sets us apart',
      valuesTitle: 'Education without borders',
      valuesDescription: 'WBS is more than a school – a community shaping future global citizens.',
      ctaTitle: 'Enroll your child at WBS',
      ctaDescription: 'Join a community that has been connecting cultures for nearly 50 years.',
      ctaPrimary: 'Schedule a visit',
      ctaSecondary: 'Download prospectus',
    },
  };

  const c = content[locale as keyof typeof content] || content.pl;

  const values = [
    { key: 'bilingual', icon: '🌍' },
    { key: 'international', icon: '📚' },
    { key: 'teachers', icon: '👨‍🏫' },
    { key: 'facilities', icon: '🏫' },
    { key: 'community', icon: '🤝' },
    { key: 'excellence', icon: '🏆' },
  ];

  return (
    <>
      <SkipLink text={t('common.skipToContent')} />
      <Header lang={locale} />
      <main id="main-content">
        <Hero lang={locale} />
        <QuickLinks lang={locale} />
        
        {/* About Section - Editorial layout inspired by Bryanston */}
        <section className="py-24 lg:py-32 bg-white overflow-hidden">
          <div className="container-custom">
            <div className="grid lg:grid-cols-12 gap-12 lg:gap-16 items-center">
              {/* Image side */}
              <div className="lg:col-span-6 relative">
                <div className="relative">
                  {/* Main image */}
                  <div className="relative rounded-3xl overflow-hidden aspect-[4/3] bg-neutral-100">
                    <div className="absolute inset-0 flex items-center justify-center text-neutral-400">
                      <span>School building photo</span>
                    </div>
                  </div>
                  {/* Accent element */}
                  <div className="absolute -bottom-6 -right-6 w-32 h-32 bg-red-600 rounded-2xl -z-10" />
                  {/* Stats card */}
                  <div className="absolute -bottom-4 -left-4 bg-white rounded-2xl shadow-xl p-6 border border-neutral-100">
                    <p className="text-3xl font-bold text-red-600">1978</p>
                    <p className="text-sm text-neutral-600">{locale === 'pl' ? 'Rok założenia' : locale === 'de' ? 'Gründungsjahr' : 'Founded'}</p>
                  </div>
                </div>
              </div>

              {/* Content side */}
              <div className="lg:col-span-6">
                <span className="inline-block px-4 py-1.5 bg-red-50 text-red-700 text-sm font-medium rounded-full mb-6">
                  {c.aboutLabel}
                </span>
                <h2 className="text-4xl md:text-5xl font-bold text-black leading-tight mb-6 whitespace-pre-line">
                  {c.aboutTitle}
                </h2>
                <p className="text-lg text-neutral-600 mb-8 leading-relaxed">
                  {c.aboutDescription}
                </p>
                <a 
                  href={`/${locale}/about`} 
                  className="group inline-flex items-center gap-2 text-red-600 font-semibold hover:text-red-700 transition-colors"
                >
                  {c.aboutCta}
                  <ArrowRight className="w-4 h-4 group-hover:translate-x-1 transition-transform" />
                </a>
              </div>
            </div>
          </div>
        </section>

        {/* Values Section - Clean card grid */}
        <section className="py-24 lg:py-32 bg-neutral-50">
          <div className="container-custom">
            <div className="text-center max-w-3xl mx-auto mb-16">
              <span className="inline-block px-4 py-1.5 bg-white text-red-700 text-sm font-medium rounded-full border border-red-100 mb-6">
                {c.valuesLabel}
              </span>
              <h2 className="text-4xl md:text-5xl font-bold text-black mb-6">
                {c.valuesTitle}
              </h2>
              <p className="text-lg text-neutral-600">
                {c.valuesDescription}
              </p>
            </div>

            <div className="grid md:grid-cols-2 lg:grid-cols-3 gap-6">
              {values.map((value, index) => (
                <div
                  key={value.key}
                  className="group bg-white rounded-2xl p-8 border border-neutral-100 hover:border-red-100 hover:shadow-xl hover:shadow-red-900/5 transition-all duration-300"
                >
                  <div className="w-14 h-14 bg-red-50 rounded-xl flex items-center justify-center mb-6 group-hover:bg-red-600 transition-colors duration-300">
                    <span className="text-2xl group-hover:scale-110 transition-transform duration-300">{value.icon}</span>
                  </div>
                  <h3 className="text-xl font-bold text-black mb-3">
                    {t(`homepage.values.${value.key}.title`)}
                  </h3>
                  <p className="text-neutral-600 leading-relaxed">
                    {t(`homepage.values.${value.key}.description`)}
                  </p>
                </div>
              ))}
            </div>
          </div>
        </section>

        {/* News Section - Latest Articles */}
        <section className="py-24 lg:py-32 bg-white">
          <div className="container-custom">
            <div className="text-center mb-16">
              <span className="inline-block px-4 py-1.5 bg-red-50 text-red-700 text-sm font-medium rounded-full mb-6">
                {locale === 'pl' ? 'Aktualności' : locale === 'de' ? 'Aktuelles' : 'News'}
              </span>
              <h2 className="text-4xl md:text-5xl font-bold text-neutral-900 mb-6">
                {locale === 'pl' ? 'Najnowsze wiadomości' : locale === 'de' ? 'Neueste Nachrichten' : 'Latest News'}
              </h2>
              <p className="text-lg text-neutral-600 max-w-2xl mx-auto">
                {locale === 'pl' 
                  ? 'Bądź na bieżąco z wydarzeniami w naszej szkole' 
                  : locale === 'de' 
                  ? 'Bleiben Sie über die Ereignisse in unserer Schule informiert'
                  : 'Stay up to date with school events'}
              </p>
            </div>

            <div className="grid md:grid-cols-3 gap-8 mb-12">
              {/* Sample News Cards - Replace with dynamic content */}
              {[
                {
                  title: locale === 'pl' ? 'Zakończenie roku szkolnego 2024/2025' : locale === 'de' ? 'Ende des Schuljahres 2024/2025' : 'End of School Year 2024/2025',
                  excerpt: locale === 'pl' ? 'Uroczyste zakończenie roku szkolnego odbędzie się 27 czerwca.' : locale === 'de' ? 'Die feierliche Beendigung des Schuljahres findet am 27. Juni statt.' : 'The ceremonial end of school year will take place on June 27.',
                  date: '20 czerwca 2025',
                  category: locale === 'pl' ? 'Wydarzenia' : locale === 'de' ? 'Veranstaltungen' : 'Events'
                },
                {
                  title: locale === 'pl' ? 'Nowa pracownia komputerowa' : locale === 'de' ? 'Neuer Computerraum' : 'New Computer Lab',
                  excerpt: locale === 'pl' ? 'Otwarcie nowoczesnej pracowni komputerowej dla uczniów.' : locale === 'de' ? 'Eröffnung eines modernen Computerraums für Schüler.' : 'Opening of a modern computer lab for students.',
                  date: '15 września 2025',
                  category: locale === 'pl' ? 'Aktualności' : locale === 'de' ? 'Nachrichten' : 'News'
                },
                {
                  title: locale === 'pl' ? 'Sukces naszych uczniów w olimpiadzie' : locale === 'de' ? 'Erfolg unserer Schüler bei der Olympiade' : 'Our Students Success in Olympiad',
                  excerpt: locale === 'pl' ? 'Uczniowie WBS zdobyli pierwsze miejsca w konkursach przedmiotowych.' : locale === 'de' ? 'WBS-Schüler gewannen erste Plätze bei Fachwettbewerben.' : 'WBS students won first places in subject competitions.',
                  date: '10 maja 2025',
                  category: locale === 'pl' ? 'Osiągnięcia' : locale === 'de' ? 'Erfolge' : 'Achievements'
                }
              ].map((news, idx) => (
                <article key={idx} className="bg-white rounded-xl overflow-hidden shadow-md hover:shadow-xl transition-all duration-300 group">
                  <div className="relative h-48 bg-gradient-to-br from-red-100 to-neutral-100 overflow-hidden">
                    <div className="absolute inset-0 flex items-center justify-center text-neutral-400 group-hover:scale-110 transition-transform duration-300">
                      <Lightbulb className="w-16 h-16 opacity-50" />
                    </div>
                    <span className="absolute top-4 left-4 px-3 py-1 bg-white/90 backdrop-blur text-red-700 text-xs font-medium rounded-full">
                      {news.category}
                    </span>
                  </div>
                  <div className="p-6">
                    <div className="flex items-center text-sm text-neutral-500 mb-3">
                      <Calendar className="w-4 h-4 mr-2" />
                      <time>{news.date}</time>
                    </div>
                    <h3 className="text-xl font-bold text-neutral-900 mb-3 line-clamp-2 group-hover:text-red-600 transition-colors">
                      {news.title}
                    </h3>
                    <p className="text-neutral-600 mb-4 line-clamp-3">{news.excerpt}</p>
                    <Link href="/pl/news" className="inline-flex items-center text-red-600 hover:text-red-700 font-medium text-sm transition-colors">
                      {locale === 'pl' ? 'Czytaj więcej' : locale === 'de' ? 'Mehr lesen' : 'Read more'}
                      <ArrowRight className="w-4 h-4 ml-1" />
                    </Link>
                  </div>
                </article>
              ))}
            </div>

            <div className="text-center">
              <Link href="/pl/news" className="inline-flex items-center gap-2 px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-full transition-all hover:shadow-lg hover:scale-105">
                {locale === 'pl' ? 'Zobacz wszystkie aktualności' : locale === 'de' ? 'Alle Nachrichten anzeigen' : 'View all news'}
                <ArrowRight className="w-5 h-5" />
              </Link>
            </div>
          </div>
        </section>

        {/* Events Section - Upcoming Events */}
        <section className="py-24 lg:py-32 bg-neutral-50">
          <div className="container-custom">
            <div className="text-center mb-16">
              <span className="inline-block px-4 py-1.5 bg-white text-red-700 text-sm font-medium rounded-full border border-red-100 mb-6">
                {locale === 'pl' ? 'Wydarzenia' : locale === 'de' ? 'Veranstaltungen' : 'Events'}
              </span>
              <h2 className="text-4xl md:text-5xl font-bold text-neutral-900 mb-6">
                {locale === 'pl' ? 'Nadchodzące wydarzenia' : locale === 'de' ? 'Kommende Veranstaltungen' : 'Upcoming Events'}
              </h2>
              <p className="text-lg text-neutral-600 max-w-2xl mx-auto">
                {locale === 'pl' 
                  ? 'Nie przegap ważnych wydarzeń szkolnych' 
                  : locale === 'de' 
                  ? 'Verpassen Sie keine wichtigen Schulereignisse'
                  : "Don't miss important school events"}
              </p>
            </div>

            <div className="space-y-4 mb-12">
              {/* Sample Events */}
              {[
                {
                  title: locale === 'pl' ? 'Dzień Otwarty' : locale === 'de' ? 'Tag der offenen Tür' : 'Open Day',
                  date: '15.04.2025',
                  time: '10:00 - 14:00',
                  location: locale === 'pl' ? 'Campus Główny' : locale === 'de' ? 'Hauptcampus' : 'Main Campus'
                },
                {
                  title: locale === 'pl' ? 'Koncert Wiosenny' : locale === 'de' ? 'Frühlingskonzert' : 'Spring Concert',
                  date: '22.04.2025',
                  time: '18:00',
                  location: locale === 'pl' ? 'Sala Koncertowa' : locale === 'de' ? 'Konzertsaal' : 'Concert Hall'
                },
                {
                  title: locale === 'pl' ? 'Mecz Piłkarski - WBS vs Legia' : locale === 'de' ? 'Fußballspiel - WBS vs Legia' : 'Football Match - WBS vs Legia',
                  date: '05.05.2025',
                  time: '15:00',
                  location: locale === 'pl' ? 'Boisko Szkolne' : locale === 'de' ? 'Schulspielfeld' : 'School Field'
                }
              ].map((event, idx) => (
                <div key={idx} className="bg-white rounded-xl p-6 shadow-sm hover:shadow-md transition-all duration-300 cursor-pointer group">
                  <div className="flex flex-col md:flex-row md:items-center gap-4">
                    <div className="flex-shrink-0 w-16 h-16 bg-red-100 rounded-xl flex flex-col items-center justify-center text-red-700 group-hover:bg-red-600 group-hover:text-white transition-colors">
                      <Calendar className="w-6 h-6 mb-1" />
                      <span className="text-xs font-bold">{event.date.split('.')[0]}</span>
                    </div>
                    <div className="flex-1">
                      <div className="flex items-center gap-3 mb-2">
                        <span className="px-3 py-1 bg-red-50 text-red-700 text-xs font-medium rounded-full">
                          {locale === 'pl' ? 'Wydarzenie' : locale === 'de' ? 'Veranstaltung' : 'Event'}
                        </span>
                      </div>
                      <h3 className="text-lg font-semibold text-neutral-900 mb-1 group-hover:text-red-600 transition-colors">
                        {event.title}
                      </h3>
                      <div className="flex flex-wrap gap-4 text-sm text-neutral-600">
                        <span className="flex items-center gap-1">
                          <Calendar className="w-4 h-4" />
                          {event.date}
                        </span>
                        <span className="flex items-center gap-1">
                          <Users className="w-4 h-4" />
                          {event.time}
                        </span>
                        <span className="flex items-center gap-1">
                          <BookOpen className="w-4 h-4" />
                          {event.location}
                        </span>
                      </div>
                    </div>
                    <div className="flex-shrink-0">
                      <button className="px-6 py-3 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-full transition-all">
                        {locale === 'pl' ? 'Szczegóły' : locale === 'de' ? 'Details' : 'Details'}
                      </button>
                    </div>
                  </div>
                </div>
              ))}
            </div>

            <div className="text-center">
              <Link href="/pl/events" className="inline-flex items-center gap-2 px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-full transition-all hover:shadow-lg hover:scale-105">
                {locale === 'pl' ? 'Zobacz kalendarz wydarzeń' : locale === 'de' ? 'Veranstaltungskalender anzeigen' : 'View event calendar'}
                <ArrowRight className="w-5 h-5" />
              </Link>
            </div>
          </div>
        </section>

        {/* Programs Section - Academies & Activities */}
        <section className="py-24 lg:py-32 bg-white">
          <div className="container-custom">
            <div className="text-center mb-16">
              <span className="inline-block px-4 py-1.5 bg-red-50 text-red-700 text-sm font-medium rounded-full mb-6">
                {locale === 'pl' ? 'Nasze Programy' : locale === 'de' ? 'Unsere Programme' : 'Our Programs'}
              </span>
              <h2 className="text-4xl md:text-5xl font-bold text-neutral-900 mb-6">
                {locale === 'pl' ? 'Akademie i zajęcia dodatkowe' : locale === 'de' ? 'Akademien und Aktivitäten' : 'Academies & Activities'}
              </h2>
              <p className="text-lg text-neutral-600 max-w-2xl mx-auto">
                {locale === 'pl' 
                  ? 'Rozwijaj swoje pasje i talenty w naszych akademiach' 
                  : locale === 'de' 
                  ? 'Entwickeln Sie Ihre Leidenschaften und Talente in unseren Akademien'
                  : 'Develop your passions and talents in our academies'}
              </p>
            </div>

            <div className="grid md:grid-cols-2 lg:grid-cols-4 gap-8 mb-12">
              {[
                {
                  icon: Soccer,
                  title: locale === 'pl' ? 'Akademia Piłkarska' : locale === 'de' ? 'Fußballakademie' : 'Football Academy',
                  description: locale === 'pl' ? 'Treningi pod okiem profesjonalnych trenerów.' : locale === 'de' ? 'Training unter professionellen Trainern.' : 'Training under professional coaches.',
                  color: 'bg-green-500',
                  href: '/pl/students/football-academy'
                },
                {
                  icon: Music,
                  title: locale === 'pl' ? 'Akademia Muzyczna' : locale === 'de' ? 'Musikakademie' : 'Music Academy',
                  description: locale === 'pl' ? 'Zajęcia instrumentalne i zespół szkolny.' : locale === 'de' ? 'Instrumentalunterricht und Schulband.' : 'Instrumental lessons and school band.',
                  color: 'bg-purple-500',
                  href: '/pl/students/music-academy'
                },
                {
                  icon: BookOpen,
                  title: locale === 'pl' ? 'Biblioteka' : locale === 'de' ? 'Bibliothek' : 'Library',
                  description: locale === 'pl' ? 'Książki i ciche miejsce do nauki.' : locale === 'de' ? 'Bücher und ruhiger Lernort.' : 'Books and quiet study space.',
                  color: 'bg-blue-500',
                  href: '/pl/students/library'
                },
                {
                  icon: Users,
                  title: locale === 'pl' ? 'Samorząd Uczniowski' : locale === 'de' ? 'Schülervertretung' : 'Student Council',
                  description: locale === 'pl' ? 'Organizuj wydarzenia i reprezentuj uczniów.' : locale === 'de' ? 'Veranstaltungen organisieren und Schüler vertreten.' : 'Organize events and represent students.',
                  color: 'bg-orange-500',
                  href: '/pl/students/student-council'
                }
              ].map((program, idx) => (
                <Link 
                  key={idx} 
                  href={program.href}
                  className="group bg-white rounded-2xl p-8 border border-neutral-200 hover:border-red-200 hover:shadow-xl hover:shadow-red-900/5 transition-all duration-300"
                >
                  <div className={`${program.color} w-16 h-16 rounded-2xl flex items-center justify-center mb-6 group-hover:scale-110 transition-transform duration-300`}>
                    <program.icon className="w-8 h-8 text-white" />
                  </div>
                  <h3 className="text-xl font-bold text-neutral-900 mb-3 group-hover:text-red-600 transition-colors">
                    {program.title}
                  </h3>
                  <p className="text-neutral-600 leading-relaxed mb-4">
                    {program.description}
                  </p>
                  <span className="inline-flex items-center text-red-600 font-semibold group-hover:translate-x-2 transition-transform">
                    {locale === 'pl' ? 'Dowiedz się więcej' : locale === 'de' ? 'Mehr erfahren' : 'Learn more'}
                    <ArrowRight className="w-4 h-4 ml-2" />
                  </span>
                </Link>
              ))}
            </div>

            <div className="text-center">
              <Link href="/pl/students" className="inline-flex items-center gap-2 px-8 py-4 bg-red-600 hover:bg-red-700 text-white font-semibold rounded-full transition-all hover:shadow-lg hover:scale-105">
                {locale === 'pl' ? 'Zobacz wszystkie programy' : locale === 'de' ? 'Alle Programme anzeigen' : 'View all programs'}
                <ArrowRight className="w-5 h-5" />
              </Link>
            </div>
          </div>
        </section>

        {/* Testimonials Section - TEMPORARILY DISABLED */}
        {/* <TestimonialsSection testimonials={testimonials} locale={locale} /> */}

        {/* CTA Section - Bold red background */}
        <section className="py-24 lg:py-32 bg-red-600 relative overflow-hidden">
          {/* Decorative elements */}
          <div className="absolute top-0 left-0 w-64 h-64 bg-red-500 rounded-full -translate-x-1/2 -translate-y-1/2" />
          <div className="absolute bottom-0 right-0 w-96 h-96 bg-red-700 rounded-full translate-x-1/3 translate-y-1/3" />
          
          <div className="container-custom relative z-10">
            <div className="max-w-3xl mx-auto text-center">
              <h2 className="text-4xl md:text-5xl lg:text-6xl font-bold text-white mb-6">
                {c.ctaTitle}
              </h2>
              <p className="text-xl text-white/90 mb-10 leading-relaxed">
                {c.ctaDescription}
              </p>
              <div className="flex flex-col sm:flex-row gap-4 justify-center">
                <a
                  href={`/${locale}/admissions`}
                  className="group inline-flex items-center justify-center gap-2 px-8 py-4 bg-white text-red-600 font-semibold rounded-full hover:bg-neutral-100 transition-all hover:shadow-lg hover:scale-105"
                >
                  <Calendar className="w-5 h-5" />
                  {c.ctaPrimary}
                </a>
                <a
                  href={`/${locale}/prospectus.pdf`}
                  className="group inline-flex items-center justify-center gap-2 px-8 py-4 bg-red-700 text-white font-semibold rounded-full hover:bg-red-800 transition-all border border-red-500"
                >
                  <Mail className="w-5 h-5" />
                  {c.ctaSecondary}
                </a>
              </div>
            </div>
          </div>
        </section>
      </main>
      <Footer lang={locale} />
      <CookieConsent lang={locale} />
    </>
  );
}
